package CommandLineInterface;

import picocli.CommandLine;
import picocli.CommandLine.Command;
import picocli.CommandLine.Option;
import picocli.CommandLine.ArgGroup;
import run.Main;

public class CliCommand {

//    @Command(name = "report", description = "Creates a report with the desired data",
//            mixinStandardHelpOptions = true)
//    private void report(
//            @ArgGroup(exclusive = false, multiplicity = "1")
//                    ReportArgGroup argGroup
//    ) {
//        // Generate reports
//    }
//
//   public static class ReportArgGroup {
//        @Option(names = {"--period", "-p"}) String period;
//        @Option(names = "--source", arity = "0..1", defaultValue = "none") String source;
//        @Option(names = "--interview", arity = "0..1", defaultValue = "none") String status;
//        @Option(names = "--talent-funnel",
//                description = "The number of candidates who applied " +
//                        "and the number of those who accepted an internship ") boolean talentFunnel;
//        @Option(names = "--aggregation",
//                description = "Grouped by either city or technology") String aggregation;
//    }
}
